import React from 'react';
import './Spinner.scss';

const Spinner = (props) => (
  <div className="page-wrap">
    <p>Идет загрузка</p>
    <div className="wrap" />
  </div>
);

export default Spinner;
